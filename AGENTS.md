# MUDSync

GPUサーバーとの連携をおこなうための自分用コマンドラインツール

## Setup

### 前提条件

- GPUサーバーへのアクセス権限があること
- .ssh/configにGPUサーバーの接続情報が登録されていること

Install:

```
uv tool install {tool_name} # 後で決める
```

## 機能（global）

サブコマンドの説明。ツール名は後で決める。

### `config`

対話型でGPUサーバーの接続情報を設定するコマンド。inquirerPyを使って実装。

1. サーバーの設定。.ssh/configに登録されているサーバーの中から選択する。
2. サーバー上の自分のホームディレクトリのpathを入力。デフォルトは/home。/home/usernameのように指定することを想定

これらの情報は$XDG_CONFIG_HOME/{tool_name}/config.jsonに保存される。tool_nameは後で決める。

### `show`

GPUサーバーへのSSH接続情報を表示する

- ip
- サーバーのホスト名
- ユーザー名
- ssh keyのpath
- サーバー上のユーザーホームディレクトリ

## 機能（project）

### `connect`

GPUサーバーにSSH接続し、home/proj_nameディレクトリに移動する。proj_nameは現在のディレクトリ名固定。

### `manage`

同期するファイルやディレクトリを管理する。例えば、dataディレクトリは同期しないなどの設定を行う。対話実装。

プロジェクトルートのファイル一覧を表示し、矢印かhjklで操作。上下でファイル移動。ディレクトリで右を押した場合、その中に入り左を押した場合、1個上に戻る。スペースでファイルを選択して、同期するかどうかを切り替える。Enterで保存して終了。

ファイルの選択状況はどこかに保存しておく。プロジェクトルートだと少し汚くなるので、xdg configかxdg stateのどちらか。

### `sync`

ローカルのプロジェクトディレクトリとGPUサーバーのproj_homeを同期する。同期するファイルサイズの合計が大きい場合は圧縮して転送したほうがいい？

転送するのは変更があったファイルのみ。変更があったかどうかはrsyncで勝手にやってくれるなら何もしなくていいし、無理ならこちらでhashを管理するとかする。圧縮することを考えるとrsyncでってのは厳しいのか？うまく工夫して。

### `build`

GPUサーバー上でDockerイメージをビルドする。サーバー上でdocker build .を実行するだけ。コンテナ名はuser_home_proj_nameのようにする。

### `run [command]`

GPUサーバーのdockerコンテナ上で指定したコマンドを実行する。例えば、`run "python train.py"`のように使用する。

### `jupyter`

GPUサーバー上でJupyter Labを起動し、カーネルのurlを出力する。`run jupyter lab`を少し使いやすくしたもの。


