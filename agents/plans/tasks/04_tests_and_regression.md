## Task 04: テスト整備と回帰確認

### 目的
- 未検証だった `run` / `jupyter` の挙動を回帰可能にする。

### 作業
- subprocess モックによる単体テストを追加
- コマンド生成と分岐の期待値テストを追加
- 既存コマンド群への副作用がないか最低限の回帰確認

### 完了条件
- 追加テストが安定して通る
- `build` 削除後も CLI 全体の動作に破綻がない
