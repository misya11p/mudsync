services:
  app:
    build: .
    working_dir: /app
    command: python train.py
    volumes:
      - .:/app
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
